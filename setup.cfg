# setuptools config file kept intentionally minimal; metadata is in pyproject.toml (PEP 621)

[options]
packages = find:
include_package_data = True

[options.packages.find]
exclude =
    tests
    tests.*

